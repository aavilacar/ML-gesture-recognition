The code was written on MATLAB R2020b.

To use the 'testing_code_endpoint_detection.m' code, uncomment the last part of the 'endpoint_detection.m' function ('wl = [wl start_point finish_point thr_start thr_finish]; gest_ch = [gest_ch; gest_ch_quantised];').
